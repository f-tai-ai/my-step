#include<stdio.h>
int main(){
  double a=0,b=0,c=0;
  printf("身長（メートル）を入力：");
  scanf("%lf",&a);
  printf("体重を入力：");
  scanf("%lf",&b);
  c=b/(a*a);
  if(c>=25){
    printf("肥満\n");
  }
    else{
      printf("普通\n");
    }
}
